### REMM2K 코딩테스트

해당 과제 수행 내용을 바탕으로 기술면접이 온라인(Google Hangout) 또는 오프라인으로 진행됩니다.

모든 소스코드는 git을 이용하여 업로드 되어야 하며 본인이 생각하는 issue단위로 commit하는것을 권장합니다.
과제 시작과 동시에 github repository URL을 저에게 공유해주시기 바랍니다.

본 과제의 예상 평균 수행시간은 6시간 ~ 12시간 이며, 과제 제출은 이메일 발송후 최대 48시간으로 제한합니다.
(수행시간은 짧던 길던 48시간을 넘기지 않는다면 평가에 영향을 끼치지 않습니다.)

1. 필수과제

[개발 가이드]
-- github io(https://pages.github.com/)에 새로운 프로젝트를 생성합니다.
-- contents.json을 git에 업로드 합니다.
-- result.png 파일을 바탕으로 index.html, layout.css, main.js 파일을 작성합니다. (꼭 분리되어야 함)
-- 샘플 결과물은 result 폴더에 이미지 형식으로 첨부되어있습니다.
   위 내용을 바탕으로, contents.json을 동적으로 읽어와 (Ajax 사용), Dynamic DOM (jQuery append) 을 이용하여 웹페이지를 구성합니다.

-- PC 환경에서는 3칸씩 1줄을 차지합니다.
-- 타블렛 환경에서는 2칸씩 1줄을 차지합니다.
-- 모바일 환경에서는 1칸씩 1줄을 차지합니다.
-- 마우스가 오버되면 사진의 크기는 10% 커져야 하며, 검정색 반투명 overlay가 활성화 됩니다.


JSON 구조 설명
-- size : column 몇개를 차지할 것인가
-- type : 본 과제에서는 사용하지 않음
-- img : 이미지 파일의 경로 또는 URL (임의 변경 가능)
-- title : 제목
-- desc : 부제목
-- url : 클릭시 이동할 URL
실제 테스트시에는 다른 JSON파일이 입력됩니다.
* 한줄이 완성되지 않을 수 있으며, 이때는 console.log로 에러정보를 출력해야함
  ex) 3, 2, 2로 배치되는 경우 마지막의 2로 인해 한줄을 채울 수 없음 


아래 크기는 참고임, 크기는 본인생각에 보기좋은 비율로 맞춰도 무관함
-- 모든 컨텐츠의 세로 크기는 370px입니다.
-- 1칸의 가로크기는 370px, 2칸은 775px, 3칸은 1180px 로 고정입니다.
-- 해당 스크린샷은 가로 1440px 에서 촬영되었습니다.


2. 선택과제

본 테스트는 "신규 기술"에 대한 이해도를 중점적으로 보기 때문에
완성된 결과물을 보여줄 필요가 없으며,
오직 주어진 내용을 바탕으로 수행하는 과정을 지켜보기 위함입니다.

만약 부담되신다면 과제를 수행하지 않으셔도 무관하며, 당락과는 상관없이 연봉협상시 평가자료로만 이용됩니다.

-- Node.js 또는 Python을 이용하여 contents.json 파일을 읽어 옵니다.
-- 해당 내용을 바탕으로 index.html을 구성하도록 합니다.
-- 어떠한 Javascript도 추가되어서는 안됩니다.
   (tip) jQuery를 이용하여 live로 구동하는것이 아니라, 컴파일 하듯이 해당 소스코드를 실행하면 index.html파일이 return되어야 함
